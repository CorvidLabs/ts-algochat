schema_version = 1
profile = "standard"

[lifecycle]
command = ["fledge", "lanes", "run", "verify"]

[contract]
enabled = true
require_coverage = 0
skip_reason = "Existing TypeScript AlgoChat behavior has no prior committed SpecSync coverage threshold; adoption begins in advisory mode"

[risk]
threshold = "block"

[provenance]
mode = "soft"
policy = ".attest.json"
skip_reason = ""

[atlas]
enabled = false
skip_reason = "Standalone documentation Pages publication remains independently managed"
